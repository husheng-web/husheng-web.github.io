# 首页 Hero 编辑式重构 — 计划

1. 更新 Header 品牌锁定和 Hero 的双语文案，保留既有导航与路由。
2. 重组 Hero 为标题、视觉、介绍 / CTA 三个层级，并接入现有减少动效支持。
3. 以 Hero 专属 CSS 实现共享栅格、人物融合、点阵与三端布局，不改全局 token。
4. 在 Chrome 检查桌面与移动首屏，完成 lint、typecheck、build 和视觉 QA。
