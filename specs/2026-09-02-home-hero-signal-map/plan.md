# Home hero signal map — plan

1. Add an accessible, localized process-map element to the existing hero using dictionary build-step data.
2. Add responsive, reduced-motion-safe styling with existing design tokens.
3. Validate type checks, lint, production build, and desktop/mobile rendered views.
