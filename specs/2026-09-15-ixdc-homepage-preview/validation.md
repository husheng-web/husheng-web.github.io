# 验证

## 自动检查

- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run build`

## 人工检查

- [x] Chrome 桌面端：项目卡片显示首页封面，弹窗显示完整首页截图。
- [x] Chrome 移动端：375px 宽度下，截图等比缩放为约 326px 宽，弹窗 `scrollWidth` 与 `clientWidth` 均为 375px，无横向溢出。
