# 验证

## 自动检查

- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run build`

## 人工检查

- [x] Chrome 桌面端：AI 学术助手弹窗依序展示 02–09 模块。
- [x] Chrome 移动端：375px 宽度下，八张图片均等比缩放为约 299px 宽，弹窗 `scrollWidth` 与 `clientWidth` 均为 375px，无横向溢出。
