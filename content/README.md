# Portfolio Content

Approved project narratives will live under `content/projects/[slug]/{zh,en}.mdx`.

No case-study source files are created until their factual content and permitted media are supplied. Project routing and pending status are controlled by `data/projects.ts`.
